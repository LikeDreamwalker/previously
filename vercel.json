{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "functions": {
    "src/app/api/chat/**/*": {
      "supportsCancellation": true
    }
  },
  "ignoreCommand": "git diff --name-only HEAD^ HEAD 2>/dev/null | grep -qvE '^(memory|tasks|sessions)/' && exit 1 || exit 0"
}
